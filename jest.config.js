// import type { Config } from "jest";

const config = {
  verbose: true,
  // preset: "ts-jest",
  testEnvironment: "node",
  roots: ["./"], // Adjust this based on your project structure
};

export default config;
